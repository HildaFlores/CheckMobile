<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prueba.CheckMobile">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Vehiculo.VehiculoActivity"
            android:label="@string/title_activity_vehiculo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Cliente.ClienteActivity"
            android:label="@string/title_activity_cliente"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Vehiculo.ListaMarcaActivity"
            android:label="Marcas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Inspeccion.InspeccionActivity"
            android:label="@string/title_activity_inspeccion"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>